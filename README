This is a test program to plot the mandelbrot set using a special coloring scheme. 
To zoom, use the mouse to mark an are to zoom, or use the +/- buttons. 
It is very simple, so performance is subpar and the graphics is not super-smooth.